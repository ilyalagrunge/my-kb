on:
 pull_request:
    types: [opened, reopened]
    branches:
      - main   
 workflow_dispatch:
 push:
    branches:    
      - 'main'
    paths:
      - '.github/**'
      - 'config/**'
      - 'build.yaml'

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main